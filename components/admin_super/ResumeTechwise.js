"use client";
import SuperResumeTechwisePage from "@/app/admin_super/resume-techwise/page"; // ✅ Import resume-techwise page route

const SuperResumeTechwiseContent = () => {
  return (
    <div className="w-10/6 p-10">
      <SuperResumeTechwisePage />
    </div>
  );
};

export default SuperResumeTechwiseContent;